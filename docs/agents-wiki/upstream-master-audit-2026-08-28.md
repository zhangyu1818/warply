# Upstream Master Audit 2026-08-28

## Scope

- Current fork before this audit: `428664fbc` (`main`, `v2026.08.27`).
- Upstream source reviewed: `511b952c2..upstream/master` (10 commits, tip `c2e4ee491`).
- Result: 4 commits ported (`fc4d563b8` native shell completions for all shells, `d5d12d90f` their integration tests, `b7ec0fc55` Agent Mode user-query timestamps, `2a183d552` secret_redaction/regex_dfas crate split), 6 rejected or not applicable (multi-team P3c LLMPreferences, dependabot CI bump, skills lock, credits/dollars toggle, docker_registry managed secret, share-modal lifecycle fix). Two pre-existing `grid_handler_test` failures (stale column arithmetic from the fork's URL localization) were fixed alongside.

## Commit-by-commit triage

| Commit | Title | Decision | Notes |
| --- | --- | --- | --- |
| `fc4d563b8` | Native shell completions: drive all shells through in-band generators (#15573) | **Accept (ported, adapted)** | Retained local terminal feature: zsh, bash, fish and PowerShell compute completions in the live session over OSC 9280 through one in-band generator-command path, replacing the zsh-only Ctrl-Y round trip. Adds `CompletionSources` dispatch (`WarpThenNative`/`WarpOnly`/`NativeOnly`/`None`) from two Terminal Input settings (`warp_completions_enabled`, `native_shell_completions_enabled`), hex-encoded OSC payloads, shell-reported replacement spans (`9280;S`), the completer punctuation-guard matcher fix, `Span::clamped_to` char-boundary clamp, control-character stripping on accepted completions, `=`-suffix space suppression, and the bootstrap encoders/generators for all four shells (bash `read -ra` + compspec resolution incl. cobra padded-shape splitting, zsh `select`-driven ZLE capture with per-request dedup and byte-offset spans, fish `complete -C`, pwsh `CommandCompletion::CompleteInput` with UTF-16→UTF-8 offset conversion). Ported via three-way patches per file with path remapping (`crates/warp_terminal/src/model/{ansi,completions}` → `app/src/terminal/model/{ansi,completions}`, test files renamed to fork conventions). Fork adaptations recorded below. |
| `ba9bcd3f4` | [multi-team P3c] Move LLMPreferences off its flat catalog onto Team/Workspace (#15567) | **Reject** | Multi-team stack over fork-absent surfaces (`UserWorkspaces` team-scoped settings, `agent_sdk`, `warp_tui`, `workspaces/gql_convert`, `warp_server_client`). Consistent with the standing multi-team rejections (×8 in the 2026-08-27 audit). |
| `d5d12d90f` | Integration tests for native shell completions (CORE-3794) (#15425) | **Accept (ported, adapted)** | Six integration tests in `crates/integration` covering the client↔shell seam per shell: menu contents, clean command run after a completion request, specs-first dispatch, and PowerShell member access. Adaptations: `TranscriptScope::Terminal` → fork's `AgentViewState::Inactive` (with `AgentViewState` re-exported from `terminal::model::block` to mirror upstream's `block::TranscriptScope` import path), `warpui_core` imports → `warpui`, fork-absent `notebooks` module dropped from `test.rs` wiring. Runtime execution impossible in this headless session (pre-existing tests fail identically with "no frames were rendered"); compile-verified and the dispatch logic is covered by the ported unit tests. |
| `ead591e4d` | Build(deps): Bump namespacelabs/nscloud-cache-action 1.6.0 → 1.6.1 (#14910) | **Not applicable** | Touches `.github/workflows/feature_flag_cleanup.yml`, which the fork does not carry (fork keeps only its own `ci.yml`/`create_release.yml`). |
| `f8c8a4881` | Update common skills lock for warpdotdev/common-skills#80 (#15582) | **Not applicable** | `skills-lock.json` is fork-absent; bundled skills remain a rejected surface. |
| `2a183d552` | Extract secret detection into secret_redaction and regex_dfas crates (#15462) | **Accept (ported, adapted)** | Compile-time crate split applied to the fork's pre-split layout: `regex_dfas` owns the DFA core (`FindConfig`, `RegexDFAs`, `replace_unicode_word_boundaries`, `get_forward`/`get_reverse` pool accessors); `secret_redaction` owns `SECRETS_REGEX`, `SecretsRegex`, `SecretLevel`, `set_user_and_enterprise_secret_regexes`, `find_secrets_in_text*`, `merge_sorted_ranges_with_levels`, `redact_secrets`, and the default regex catalog; `SecretRange` → `StringRange` moved from warpui_core into string-offset; the grid-search loop stays on `GridHandler` taking `&RegexDFAs` (the fork's delegating wrappers in `grid_handler.rs` were removed). The crate carries upstream's newer catalog state (`WARP_API_KEY`, `FIREBASE_AUTH_DOMAIN`, `DefaultRegex` names) and the ASCII word-boundary rewrite for secret patterns (the fork previously passed `enable_unicode_word_boundary=true`). `warp_errors::report_error!` adapted to `log::error!`; the fork's blocklist-local `find_secrets_in_text*` copies replaced by crate re-exports; `agent/redaction.rs` reduces to a re-export (the fork has no `redact_inputs` server path); `secrets_test.rs` superseded by the crate's `lib_tests.rs` (git records the 95% rename). |
| `787c86975` | Add Credits/Dollars usage display unit toggle to Appearance settings (#15533) | **Not applicable** | Entirely about usage/spend displays over removed billing surfaces: the fork has no `format_credits_with_cost`, `conversation_details_panel` credits row, `billing_and_usage` pages, `PricingTransparency` flag, or `UsageDisplayUnit` anchor symbols. |
| `8731eee3c` | REMOTE-1625 PR 7a: docker_registry typed managed-secret encryptor (#15418) | **Reject** | Managed secrets (`crates/managed_secrets`, `crates/graphql`, `managed_secrets_wasm`) are removed surfaces; the client half of Warp-hosted runner registry credentials has no local counterpart. |
| `b7ec0fc55` | Add timestamps to Agent Mode user queries (#15605) | **Accept (ported, adapted)** | Local AgentView UI: `format_message_timestamp`/`is_trustworthy_message_timestamp` in `util::time_format` (+ tests), `AIBlockModel::query_sent_at` fed from the exchange's `start_time`, a hover tooltip ("Message sent …"), and a "Copy timestamp" overflow-menu item (`CopyAIBlockTimestamp`); missing/default timestamps omit both affordances. Adaptations: the fork's query renderer has no avatar element (upstream's unported avatar feature), so the tooltip anchors on the query container with upstream's exact `overlay_tool_tip_on_element` call; upstream's `terminal/view/context_menu.rs` menu hunk lands in the fork's inline menu builder in `view.rs`; only `query_sent_at` is ported (`time_since_request_start`/`was_autodetected_ai_query` in the same upstream context belong to unported upstream work); the `warp_completer` let-chains clippy cleanup included verbatim. |
| `c2e4ee491` | fix: avoid rendering share modal without a session (#13942) | **Not applicable** | All destination symbols (`ShareModalOpened`, `share_block_modal`, `terminal_with_open_share_block_modal`) are absent: the shared-block share modal is a removed cloud-sharing surface. |

## Fork adaptation details for `fc4d563b8`

- **Feature flag**: `FeatureFlag::NativeShellCompletions` added to `crates/warp_features` (upstream enum placement/doc) and wired through the fork's cargo-feature mechanism — `native_shell_completions = []` in `app/Cargo.toml` **and in `default`** plus the `#[cfg]` arm in `app/src/lib.rs::enabled_features()`, matching the fork's retained-feature pattern (e.g. `ime_marked_text`). Upstream's `DOGFOOD_FLAGS` entry is not applicable: the fork's flag pipeline is cargo-feature-driven and the retained feature runs ungated.
- **Path remapping**: upstream's post-#14875 paths `crates/warp_terminal/src/model/{ansi/mod.rs, ansi/handler.rs, completions.rs, ansi/mod_tests.rs}` were applied to the fork's pre-split files `app/src/terminal/model/{ansi/mod.rs, ansi/handler.rs, completions.rs, ansi/mod_test.rs}` with `crate::model::` imports rewritten to `crate::terminal::model::`.
- **`features_page.rs`**: ported to the fork's widget pattern — no `TelemetryEvent::FeaturesPageAction` arms, `log_setting_result` instead of `report_if_error!`, no `LocalOnlyIconState`, no `with_enabled` (the flag is default-on). The two `ToggleSettingActionPair` entries, action variants, handlers, the flag-gated widget push (falling back to `CompletionsMenuWhileTypingWidget` when off), and the `WarpCompletionsWidget` (with nested as-you-type subgroup) / `NativeShellCompletionsWidget` (with press-Tab description) render verbatim.
- **Settings entries**: `sync_to_cloud`/`surface` fields dropped (fork's settings macro has no cloud sync); both settings default `true` as upstream.
- **`pty_controller.rs`**: kept the fork's tmux 5-tuple `send_write_to_event_loop` (no `did_write` return) and `before_write_fn: Option<Box<dyn Fn()>>`; the new `RunNativeShellCompletions` write arm uses the fork's `start_active_block_for_in_band_command` before-write hook instead of upstream's `start_in_band_command_execution`/`StartCommandOutcome` (fork lacks the lifecycle coordinator). `execute_next_queued_write` treats the completions write as a command per upstream's `is_command` match; the `!did_write` half is not applicable since the fork's write path never declines.
- **`terminal_model.rs`**: fork's `send_terminal_event` kept (upstream: `send_app_event`); fork's tmux handler arms preserved in `model_events.rs`.
- **pwsh.ps1**: export list gains `Warp-Run-GeneratorCommand-NativeCompletion` while keeping the fork's removal of `Warp-Finish-Update`/`Warp-Handle-DistUpgrade`.
- **Test infrastructure**: `pty_controller_lifecycle_tests.rs` created new, declared inside `pty_controller.rs` like upstream's, containing only the two native-completions tests plus scaffolding (`TestEventLoopSender`; `terminal_model()` via the fork's `TerminalModel::mock`) — upstream's remaining lifecycle tests in that file require the fork-absent coordinator APIs. `input_test.rs`'s `initialize_app` registers `InputClassifierModel` (the fork's git-spec completion path reaches the classifier; upstream's test env differs). The fork's `test_completions_while_typing_doesnt_hide_autosuggestion` was preserved where upstream context expected `test_tab_completion_with_multibyte_chars`.
- **Bootstrap scripts**: `bash -n`/`zsh -n` verified locally; fish/pwsh not installed in this session — applied as byte-exact upstream text (upstream's own 582-case differential validated them).

## Pre-existing test failures fixed alongside

Both failed identically on `main` before this merge (stale expectations from the fork's earlier URL localization `warp.dev`→`warply.local`/`example`, which lengthened strings without updating column arithmetic):

- `test_secrets_serialization` (grid_handler_test): marked secret range widened to the full 17-char `zach@warply.local` (cols 4..=20), bounds to 0..=24, star count 13→17. The old expectations produced a partial-plaintext render (`*************ocal `).
- `test_find_url_omits_trailing_periods`: the `github.com/example/project/issues` URL spans cols 6..=46 (41 chars), not 6..=44; the interior-point probe moved to col 40.

Also removed four pre-existing unused imports surfaced by the build (`fmt::Display`, `uuid::Uuid` in `agent/conversation.rs`; `fmt::Display`, `ops::Deref`, `serde::{Deserialize, Serialize}` in `agent/task.rs`).

## Provenance

- `fc4d563b8`: per-file `git diff fc4d563b8^ fc4d563b8 -- <path> | git apply --3way` for same-path files; remapped-path patches for the `crates/warp_terminal` → `app/src/terminal/model` set; `features_page.rs` and the two import-heavy files applied by hand against the upstream hunks. Omitted upstream paths/hunks: `app/src/features.rs` `enabled_features` (fork's lives in `app/src/lib.rs`), `DOGFOOD_FLAGS` entry, `.with_enabled(...)` gating, `TelemetryEvent` arms, `LocalOnlyIconState` args, `sync_to_cloud`/`surface` setting fields, MSYS2/`wsl_name` bash branches (already absent), pwsh `Finish-Update`/`DistUpgrade` exports, upstream lifecycle-stack tests in `terminal_model_tests.rs` and `pty_controller_lifecycle_tests.rs` (fork-absent `StartCommandOutcome`/lifecycle coordinator/`OrderedTerminalEventType` machinery), and `terminal_surface.rs`/`PtyIntent` (fork dispatches the view event through `terminal_manager_util.rs`, which needed no change).
- `d5d12d90f`: wiring patches applied three-way; new test file copied from upstream with the three recorded substitutions.
- `b7ec0fc55`: three-way patches for `time_format*`, `v2.rs`, `block.rs`, `model.rs`, `model_impl.rs`, `view_impl.rs`, `query.rs`, `view.rs`; the `context_menu.rs` hunk hand-applied to the fork's inline menu builder.
- `2a183d552`: `regex_dfas`/`secret_redaction` crate files copied from upstream (`report_error!` → `log::error!`); `find.rs` rewritten to upstream's post-split GridHandler shape with fork import paths; `secrets.rs` reduced to crate re-exports + the kept grid-obfuscation items; `grid/secrets.rs` import retargeted; `SecretRange`→`StringRange` rename applied across `string-offset`, `warpui_core` elements, `blocklist/block.rs`, and the blocklist secret-redaction state/tests.

## Verification

- `bash -n`/`zsh -n` on the changed bootstrap scripts: clean.
- `cargo fmt -- --check`: clean.
- `cargo check -p warp --all-targets --message-format short`: pass (no errors, no new warnings).
- `cargo check --workspace --all-targets --message-format short`: pass.
- `cargo nextest run -p warp -E 'test(slash_command) | test(acp) | test(terminal_suggestions)'`: 156/156 pass (unchanged suite size).
- New/affected unit suites: native completions dispatch tests (`native_completions_after_empty_specs_bails_when_stale`, `input_tab_*`, `resolve_completion_sources` mod tests, `native_shell_replacement_span_*`), terminal-model OSC tests (`completions_are_emitted_in_the_order_the_shell_sent_them`, `completion_replacement_span_saturates…`), pty lifecycle tests (`native_shell_completions_*` ×2), `time_format` tests (6/6), `secret_redaction`+`regex_dfas`+`string-offset` (22/22), secret/find/regex selection (115/115 incl. the two fixed pre-existing failures), `warp_completer` full + `--features v2` (120/120 + 4 skipped): all pass.
- Integration suite: compile-verified; not runnable headless in this session (pre-existing `test_single_command` fails the same way — "no frames were rendered").
- Deletion-surface scans over `main...HEAD` (removed product surfaces, MCP/skills symbols, Linux/Windows/WASM platform branches): zero added hits.
- Final `cargo build --workspace --all-targets --message-format short`: pass; `cargo clean` run after the release push.
- `CARGO_PROFILE_DEV_DEBUG=0` used for check/test/build as in prior audits.
