# Upstream Master Audit 2026-08-22

## Scope

- Current fork before this audit: `192f13a07` (`main`, `v2026.08.21`).
- Upstream source reviewed: `8936686f2..upstream/master` (14 commits, tip `9e8ba7341`).
- Result: 6 commits accepted or adapted (IME marked text by default, right-click Shift hint, deferred-enter test margins, settings trailing-element framework, Alt+1 fixed-binding removal, grep NUL-delimited parsing fix), 8 rejected or not applicable (MCP PATH/agent-SDK/managed-MCP plumbing, Oz env aliases, Warp Agent page rewrite, Oz API-keys section rename, team-scoped code indexing, Sentry build path, usage-footer dollars).

## Commit-by-commit triage

| Commit | Title | Decision | Notes |
| --- | --- | --- | --- |
| `b4a2a8faa` | Fix stdio MCP startup for oz agent run: decouple PATH inheritance from SettingsMode (#15396) | **Reject** | The only consumer of `can_inherit_process_path_for_mcp` is `app/src/ai/mcp/templatable_manager/native.rs` (app-managed MCP startup — removed surface; directory absent). Adding the MCP-specific `ExecutionMode`/`AppExecutionMode` methods with no caller would be dead code for a rejected product path. |
| `18179177a` | Explain Shift+right-click when the right-click setting pastes (#15392) | **Accept (ported)** | Follow-up to the ported `c25ac4070` right-click setting. `RightClickBehaviorWidget::render` hoists `selection_settings` and passes `right_click_pastes().then_some("Shift+right-click to open the context menu.")` as `render_dropdown_item`'s `secondary_text`. The fork's helper already has the `secondary_text` parameter; upstream's `LocalOnlyIconState` argument stays omitted (fork argument list). |
| `fa2d43ce2` | Widen assert_eventually margin for flaky OpenCode deferred-enter test (#15394) | **Accept (ported)** | Test-only: `100 =>` widened windows in `submit_cli_agent_rich_input_opencode_defers_enter_and_close` and `submit_with_plugin_but_auto_toggle_off_respects_auto_dismiss`. Applied to the fork's `view_test.rs` (upstream filename is `view_tests.rs`); the fork's `assert_eventually!` macro already supports the `$ticks:literal =>` form. |
| `a18026275` | Support trailing elements on settings page titles and category headers (APP-5559) (#15345) | **Adapt (ported)** | See provenance below. |
| `146684ee7` | Enable IME marked text on all platforms (Windows, Linux, macOS) (#15395) | **Adapt (ported)** | Upstream adds `ime_marked_text` to the app `default` feature set and drops the `#[cfg(target_os = ...)]` guard on `FeatureFlag::ImeMarkedText` in `RELEASE_FLAGS`. The fork's `warp_features` file already registers the flag unconditionally (macOS-only host, no guard to remove) and already lists it in `RELEASE_FLAGS`/`DOGFOOD_FLAGS`, but the cargo feature was not default-on, so plain dev builds still had the marked-text editor paths disabled. The port adds `"ime_marked_text",` to the fork's `default` list, making the macOS marked-text feature default-on in every build as upstream intends. Purely local editor/input rendering; no service dependency. |
| `0e075a072` | Mirror injected OZ_* env vars to WARP_* aliases (REMOTE-2957) (#15374) | **Reject** | Entirely anchored on `app/src/ai/agent_sdk/` harness env plumbing plus `warp_managed_secrets`; `agent_sdk` is a removed surface and the fork has no `OZ_RUN_ID_ENV`/harness constants to alias. |
| `90c2484dc` | Fix hidden, non-remappable Alt+1 Project Explorer binding (#15310) | **Accept (ported)** | Removes the redundant `FixedBinding::custom(ToggleProjectExplorer, ...)` registration; the editable `workspace:left_panel_project_explorer` binding already drives both the macOS View menu hint and Settings > Keyboard shortcuts. Applied verbatim to the fork's `workspace/mod.rs` (same block existed). On the macOS-only fork the non-macOS keystroke-conversion bug cannot trigger, but the removal is upstream's current state and eliminates the duplicate registration. |
| `3f79221d1` | Make the Warp Agent settings page categorized and per-setting searchable (APP-5559) (#15372) | **Not applicable** | Rewrites `app/src/settings_view/warp_agent_page.rs` only; the file does not exist in the fork (Warp Agent settings page removed at baseline; fork AI settings live in `ai_page.rs`, already categorized/searchable). The fork did take this commit's prerequisite framework (`a18026275`). |
| `7a7cd7ad7` | Drop Oz branding from the settings API keys page (#15424) | **Not applicable** | Renames the `OzCloudAPIKeys` settings section enum/variant to `WarpCloudAgentAPIKeys` and rewords its label. The section, the API-keys page, and the "Cloud platform" settings group are removed surfaces in the fork (BYOK/cloud keys removed; terminal-suggestions API keys live under `AISettings`). |
| `155889bbf` | [multi-team PR 1C] Code indexing settings: team-scoped context (#15353) | **Reject** | Teams foundation: `app/src/workspaces/user_workspaces.rs` (absent) and `code_indexing_page.rs` (absent). Team-scoped settings context is a removed surface. |
| `391dd76ad` | Retry transient 5xx when resolving managed MCP client configs (#15426) | **Reject** | Managed MCP client config resolution under `app/src/ai/agent_sdk/` plus `app/src/server/retry_strategies.rs`; all anchors absent (agent SDK, server API client, managed MCP removed). |
| `fbbfc41f3` | Fix grep tool ParseIntError on colon-bearing paths (QUALITY-1792) (#15422) | **Adapt (ported)** | See provenance below. |
| `78d1eabdf` | Fix self-invalidating rerun-if-changed for the Sentry framework path (#15433) | **Not applicable** | The fork's `app/build.rs` has no Sentry framework path (crash-reporting infrastructure removed). |
| `9e8ba7341` | Fix usage footer conversation-total dollars to use charged usage (APP-5579) (#15423) | **Not applicable** | `charged_usage`/`usage_totals` anchors, the usage rollup (`blocklist/usage/`), and the usage footer views are absent from the fork (billing/usage surface removed; re-confirmed by grep this audit). |

## Provenance: `a18026275` port detail

- `settings_page.rs`: `render_page_title` refactor extracting `render_title_text`; `PageTitle<V>` (`text` + `trailing_element`) replacing `Option<&'static str>` on `PageType::Uncategorized`/`Categorized` and on `FilteredPageType`; `CategoryHeader<V>` (`title`/`subtitle`/`trailing_element`) with `Category.header: Option<CategoryHeader>`; `Category::new` builds the header only for a non-empty title; `with_header`/`with_subtitle`/`with_trailing_element` builders; `TrailingElementRenderer` type alias; `render_header_with_trailing_element` + `render_page_title_with_trailing` helpers; the categorized render loop now wraps each header through `render_header_with_trailing_element` and draws `render_separator` between the page title and the first category; `filter_page` maps `FilteredCategory` from the optional header (`map_or("", |h| h.title)`).
- `warpify_page.rs`: `Category::new("Subshells", …).with_subtitle(...)` becomes `Category::with_header(CategoryHeader::new("Subshells").with_subtitle(...), ...)`; import list gains `CategoryHeader`.
- Fork adaptations: upstream's standalone `search_terms_match` was **not** added — it is pre-existing upstream context (hunk header), while the fork keeps its nested fn inside `update_filter`; the categorized header branch uses the fork's 2-arg `render_sub_header` (no `LocalOnlyIconState`); `PageTitle::new` carries `#[allow(dead_code)]` because every fork page currently passes `None` (the only upstream constructor callers live in pages absent from the fork, e.g. the Account page). Upstream itself marks the other no-consumer builders (`with_trailing_element`, `with_header`) `#[allow(dead_code)]`.
- Omitted upstream paths: `cli_agents_page.rs`, `code_editor_review_page.rs`, `knowledge_page.rs`, `main_page.rs`, `privacy_page.rs`, `scripting_page.rs` (pages absent from the fork — no `Some(PageTitle::new(...))` call sites to convert) and `mod_tests.rs` (fork has no such file).

## Provenance: `fbbfc41f3` port detail

- `execute/grep.rs` core ported verbatim in structure: `GrepCommandOutcome` (success/no-matches distinction so exit 1 stops meaning "error"), `execute_grep_command` helper, `parse_null_delimited_grep_output` (NUL-delimited `path\0line\0` records, skipping unparseable records instead of failing the whole call), `run_grep_command` dispatching to the `--null`-flagged command and falling back to `run_grep_per_file_fallback` (single `grep -rl` listing captured with its exit status, then per-file `grep -n` re-reads behind NUL path markers via `build_grep_content_scan_command`), `parse_grep_content_scan_output`, `parse_single_file_grep_output`, `build_git_grep_command` gaining `-z`, `build_grep_command` gaining long `--null` (never `-Z`), and `build_select_string_command` piping `Select-String` through `ForEach-Object` into the same NUL shape.
- `execute/grep_tests.rs`: all 16 new/updated parser tests ported (`parse_null_delimited_grep_output_*`, `parse_single_file_grep_output_*`, `parse_grep_content_scan_output_*`).
- Fork adaptations: `Session::execute_command` stays the fork's 3-arg form (no `ExecuteCommandOptions`); the two telemetry functions (`create_redacted_grep_error_event`, `log_grep_error`), their call sites/clone plumbing, `TelemetryEvent::GrepToolSucceeded`, `preprocess_action` (`PreprocessActionInput`), `ServerOutputId`/`get_server_output_id`, `warp_errors::report_error!` (fork keeps `log::error!` with `{e:?}`), and the `#[cfg(not(target_family = "wasm"))]` gates are omitted per fork baseline; the `test_create_redacted_grep_error_event` telemetry test and the `FIREBASE_AUTH_DOMAIN` import were not added.
- Runtime ownership note: `GrepExecutor` is a live fork path (local tool execution through the active terminal session for CLI-agent/AgentView flows), not a restored Warp Agent backend; the fix only changes command construction and output parsing.

## Verification

- `cargo fmt -- --check`: clean after all ports.
- `cargo check -p warp --all-targets --message-format short`: pass (pre-existing warnings only; the one new `PageTitle::new` dead-code warning was resolved with upstream-style `#[allow(dead_code)]`).
- `cargo check --workspace --all-targets --message-format short`: pass.
- Focused tests: `test(grep)` 28/28 (incl. the 16 new NUL-parsing tests); `test(right_click)` + the two widened timing tests 8/8; `test(settings_view) | test(settings_page)` 12/12; standard suite `test(slash_command) | test(acp) | test(terminal_suggestions)` 156/156.
- `cargo build -p warp --all-targets --message-format short`: succeeded; `cargo clean` after the release push.
- Deletion-surface scans over the eight ported files: broad removed-area, MCP/skills, and platform scans all return zero hits.
- Disk note: `CARGO_PROFILE_DEV_DEBUG=0` used for check/test/build as in prior audits; final build/clean follows the standard workflow.
