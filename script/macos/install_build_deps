#!/bin/sh
#
# Installs macOS-specific build dependencies required to build Warply.

if xcodebuild -help 2>&1 | grep -q -- "-downloadComponent"; then
  xcodebuild -downloadComponent MetalToolchain
else
  echo "xcodebuild does not support -downloadComponent; skipping MetalToolchain download."
fi
